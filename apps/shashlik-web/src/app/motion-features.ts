/**
 * Набор фич motion для `LazyMotion`. Вынесен в отдельный модуль, чтобы
 * бандлер мог отрезать его в асинхронный чанк: сам движок анимаций нужен
 * только для перехода на страницу товара, а не для первой отрисовки витрины.
 */
export { domAnimation as default } from "motion/react"
